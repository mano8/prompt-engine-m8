-r requirements_base.txt

# Testing
pytest>=9.0.3
pytest-cov>=7.1.0
coverage>=7.15.4

# Debugging
debugpy>=1.8.20

# Linting / formatting / security
# ruff pinned exactly (A27): unpinned floors let CI resolve whatever ruff
# publishes that day, and 0.16.x enables a materially broader default rule
# set than 0.15.x, making `ruff check .` non-reproducible across two runs.
# Matches fa-auth-m8's exact-pin convention.
ruff==0.15.18
bandit>=1.9.4
mypy>=2.1.0

# Build tools (optional but OK in dev)
setuptools>=82.0.1
wheel>=0.47.0
build>=1.5.0